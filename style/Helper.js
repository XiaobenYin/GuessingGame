import { StyleSheet } from "react-native";

const color = {
    bg1: "#9932cc",
    bg2: "white",
    cardbg: "#800000",
    note: "#ffa500",
    purple: "#7b68ee",
    pink: "#ff00ff",
    gole: "gold",
}

const styles = StyleSheet.create({
    container: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
    },
    card: {

    }
  });

export {color, styles}